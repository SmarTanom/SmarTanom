import React from 'react';

export default function AdminDashboard(){
	return (
		<div style={{padding:'2rem', color:'#123'}}>
			<h1>Admin Dashboard</h1>
			<p>This is a placeholder admin dashboard. Replace with real admin controls (user management, device oversight, analytics) as backend endpoints become available.</p>
		</div>
	);
}
