# Minimal ignore file: only ignore dependency modules / environments

# Python virtual environments
.venv/
venv/
ENV/

# Node modules
frontend/node_modules/
mobile/node_modules/
node_modules/

# (Keep everything else versioned intentionally)
